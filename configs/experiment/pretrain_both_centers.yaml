# @package _global_

defaults:
  - override /datamodule/self_supervised: self_supervised_twocenter.yaml
  - override /datamodule/supervised: supervised_twocenters.yaml
  - override /datamodule/augmentations: crops_plus_ultrasound_augs

name: pretrain_both_centers